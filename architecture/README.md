# Architecture — runtime-adapt

This branch only keeps the note for the maintainer tool.

| Note | Area |
|---|---|
| [runtime-adapt.md](runtime-adapt.md) | `tools/runtime-adapt`: generate this checkout |

Green for the tool: `dub test --root=tools/runtime-adapt --compiler=ldc2`.
