# Method (carried by value)

Read the tree, not the reputation. Write understanding as cohesive paragraphs grounded in file:line. Mark anything unobserved as unobserved. Notes are addressed to the next change: loci, invariants (construction vs convention), extension points, open questions. Correct notes against the tree when a pass proves them wrong. Do not import a generic “LLVM compiler” architecture that this checkout does not implement.

On branch `tools/runtime-adapt`, keep `AGENTS*.md` and `architecture/` tracked with the tool. Runtime-adapt caches stay gitignored (`--clean` removes them).
