import platform

# Exceptions and thread locals support in jit is lacking on various platforms
config.available_features.add('JitExceptions')
config.available_features.add('JitThreadLocal')

if (platform.system() == 'Windows'):
    config.available_features.remove('JitExceptions')
